# Running frontend
cd frontend
npm run dev

# Running backend
cd backend
npm run server

Implement a slider on the right page of home that displays emotion counts for the month.
Can go back a month and forward a month, like a image slideshow